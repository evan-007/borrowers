<h1>Friends Index</h1>
{{! The context here is the controller}}
<h2>Total friends: {{length}}</h2>
<ul>
  {{#each}}
    <li>
      {{#link-to 'friends.show' this}}
        {{fullName}}
      {{/link-to}}
    </li>
  {{/each}}
</ul>
